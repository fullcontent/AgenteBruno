# NOW.md — foco atual

Snapshot do que está em foco agora. Atualizar sempre que a prioridade operacional mudar — não é histórico, é o estado presente. Para histórico, ver `logs/decisions.md`.

## Em foco

- Reestruturação do `AgenteBruno` para o formato BrunoOS (root files + `projects/` + `knowledge/` + `templates/` + `skills/` + `archive/` + `logs/`).
- Popular `projects/AventuFilm.md` e `projects/Ambialles.md` com dados reais (status, objetivos, métricas) — hoje são esqueletos.
- Adaptar `scripts/sync.sh` para sincronizar a nova estrutura com `~/.hermes/` na VPS e validar leitura pelo Hermes.
- Demais projetos (ex.: Fractional CTO, EscaladaCuritiba, SaaSDocumental) aguardando envio do Bruno para entrar em `projects/`.

## Sem data de revisão definida

Atualizar esta seção conforme os itens acima forem concluídos ou substituídos.
